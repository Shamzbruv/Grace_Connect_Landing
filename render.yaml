services:
  - type: web
    name: graceconnect-app
    runtime: static
    staticPublishPath: build/web
    buildCommand: echo "Using pre-built web output"
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
